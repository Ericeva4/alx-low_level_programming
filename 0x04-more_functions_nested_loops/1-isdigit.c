#include "main.h"
/**
 * _isdigit - This function checks foe a digit (0 to 9)
 * @c: input to be checked
 * Return: This will return 1 if c is a digit, o if otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
