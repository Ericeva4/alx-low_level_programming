#include "main.h"
/**
 * mul - This function checks for a digit (0).
 * @a: a - first variable
 * @b: b - second variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
